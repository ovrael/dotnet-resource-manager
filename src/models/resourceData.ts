export class ResourceData {
    language: string = '';
    value: string = '';

    constructor(language: string, value: string) {
        this.language = language;
        this.value = value;
    }
}